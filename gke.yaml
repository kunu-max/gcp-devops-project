apiVersion: apps/v1
kind: Deployment
metadata: 
  name: gcp-devops
spec:
  template:
     metadata:
      labels:
        app: gcp
     spec:
      containers:
      - name: gcp-devops
        image: gcr.io/noble-truck-388707/gcp-devops:latest
        ports:
        - containerPort: 5000
        env:
          - name: PORT
            value: "5000"
  replicas: 1
  selector: 
    matchLabels:
        app: gcp
      

